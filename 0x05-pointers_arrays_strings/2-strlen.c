#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function that returns the length of a string.
 *
 * @s: entry of the string
 *
 * Return: len of the string
 */

int _strlen(char *s)
{
	int len = strlen(s);
	return (len);
}
